# ---------------------------------------------------------------------------
# Giao diện web — biến môi trường (copy thành web/.env)
# Chỉ biến có tiền tố VITE_ mới được đưa vào bản build.
# ---------------------------------------------------------------------------

# Địa chỉ API. Local: http://localhost:3001
# Thật: https://api.tenmiencuaban.vn  (reverse proxy aaPanel → cổng 3001)
VITE_API_URL=http://localhost:3001
